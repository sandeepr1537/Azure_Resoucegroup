name: Destroy Resource Group in Azure using Terraform

on:
  push:
    branches:
      - main  # Trigger the workflow on pushes to the main branch (change as needed)

jobs:
  terraform:
    name: Destroy Resource Group using Terraform
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: 1.4.0  # Specify the Terraform version you want to use

    - name: Install Azure CLI
      run: |
        curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash  # Install Azure CLI on the runner

    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}  # Use the Azure credentials stored as a GitHub secret

    - name: Initialize Terraform
      run: terraform init

    - name: Validate Terraform
      run: terraform validate

    - name: Plan Terraform destruction
      run: terraform plan -destroy -out=tfplan  # Generate the destruction plan

    - name: Apply Terraform destruction
      run: terraform apply -auto-approve tfplan  # Apply the destruction plan
